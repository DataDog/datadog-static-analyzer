rulesets:
  - java-best-practices:
    rules:
      avoid-printstacktrace:
        arguments:
          # 'rulesets/rules/<rule>/arguments/<argument>' is a string or a map.
          foo:
            - bar
