[workspace]

members = [
    "bins",
    "cli",
    "kernel",
    "server"
]

[profile.release]
lto = true

[workspace.dependencies]
anyhow = "1"
base64 = "0.21.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
derive_builder = "0.12"
serde-sarif = "0.4"
