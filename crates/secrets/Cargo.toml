[package]
name = "secrets"
version = "0.0.0"
license = "Apache-2.0"
edition = "2021"

[dependencies]
secrets-core = { path = "../secrets-core" }
thiserror = "1.0.58"

[dev-dependencies]
tempfile = "3.10.1"
